export const vw = (percent) => Math.floor(window.innerWidth / 100 * percent)
export const vh = (percent) => Math.floor(window.innerHeight / 100 * percent)

export const isMobile = window.innerWidth < 768
